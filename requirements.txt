# =============================================================================
# KI-Dokumenten-Archiv – Python-Abhängigkeiten
# =============================================================================

# --- Web-Framework & API ---
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
pydantic>=2.7.0,<3.0
pydantic-settings>=2.2.0,<3.0

# --- Konfiguration ---
pyyaml>=6.0.1,<7.0

# --- PDF-Verarbeitung & OCR ---
PyMuPDF>=1.24.0,<2.0          # fitz: PDF lesen, schreiben, splitten
pdf2image>=1.17.0,<2.0        # PDF → Bild für Barcode-Erkennung
pyzbar>=0.1.9,<1.0            # Barcode/Patch-Code-Erkennung
Pillow>=10.3.0,<11.0          # Bildverarbeitung
reportlab>=4.1.0,<5.0         # PDF-Erzeugung (Patch-Code-Generator)

# --- KI / Machine Learning ---
torch>=2.3.0,<3.0             # PyTorch (GPU-Unterstützung)
transformers>=4.41.0,<5.0     # Hugging Face Transformers (LLM)
sentence-transformers>=3.0.0,<4.0  # Sentence-Embeddings (768-Dim)
accelerate>=0.30.0,<1.0       # Beschleunigung für Transformers

# --- Datenbank ---
mysql-connector-python>=8.4.0,<9.0   # MariaDB/MySQL Connector

# --- Dateisystem-Überwachung ---
watchdog>=4.0.0,<5.0          # Inotify/Polling-basierter Watcher

# --- HTTP-Client ---
requests>=2.32.0,<3.0         # Paperless-ngx REST-API

# --- Hilfsbibliotheken ---
python-multipart>=0.0.9,<1.0  # FastAPI Datei-Upload
httpx>=0.27.0,<1.0            # Async HTTP-Client (für Tests)
rich>=13.7.0,<14.0            # Formatierte Konsolenausgabe (Pre-Classifier Berichte)

